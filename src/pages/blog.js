import * as React from 'react';

import Layout from '../components/layout';

const Blog = () => {
  return (
    <Layout pageTitle='Blog'>
      <div>Blog</div>
    </Layout>
  );
};

export default Blog;
